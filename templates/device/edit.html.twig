{% extends 'base.html.twig' %}

{% block title %}Modifier{% endblock %}

{% block body %}
    <h3>Modifier {{ device.model|replace({'_': " "}) }} </h3>

    {{ include('device/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('device_index_stock') }}">Liste des smartphones</a>
    <a href="{{ path('device_index_comparateur') }}">Comparer</a>

    {{ include('device/_delete_form.html.twig') }}
{% endblock %}
