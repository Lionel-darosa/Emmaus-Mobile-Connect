{% extends 'base.html.twig' %}

{% block title %}New Device{% endblock %}

{% block body %}
    <div class="form"> 
        <h1>Calculons le prix!</h1>

        {{ include('device/_form.html.twig') }}
    </div>
    {# <a href="{{ path('device_index') }}">back to list</a> #}
{% endblock %}
