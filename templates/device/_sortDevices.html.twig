{{ form_start(form) }}
    <div class="container mt-4">
        <div class="row">
            <div class="col">
                <div class="form-check">
                    {{ form_row(form.screenSize) }}
                    {# <input type="button" name="{{ field_name(form.screenSize) }}" value="{{ field_value(form.screenSize) }}" placeholder="{{ field_label(form.screenSize) }}"/> #}
                </div>
            </div>
            
                <div class="col">
                <div class="form-check">
                    {{ form_row(form.ram) }}
                    
                </div>
            </div>
        </div>
            
        <div class="row">
          
                <div class="col">
                 <div class="form-check">
                    {{ form_row(form.storage) }}
                    
                </div>
            </div>
            <div class="col">
            
            <div class="form-check">
        {{ form_row(form.price) }}
            </div>
            </div>
        </div>
        <div class="text-center">
            <button type="submit" class="btn btn-secondary mt-2">Sauvegarder</button>
        </div
    </div>
    
{{ form_end(form) }}